# from django.urls import path

# from . import views

# app_name = 'locations'
# urlpatterns = [
    # path('', views.LocationListView.as_view(), name='location-list'),
    # path('add/', views.LocationCreateView.as_view(), name='location-create'),
    # path('<int:pk>/', views.LocationDetailView.as_view(), name='location-detail'),
    # path('<int:pk>/update', views.LocationUpdateView.as_view(), name='location-update'),
    # path('<int:pk>/delete', views.LocationDeleteView.as_view(), name='location-delete'),
# ]
